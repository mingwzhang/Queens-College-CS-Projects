#include <iostream>
using namespace std;

/*
void sayHello()
{
    cout << "Hello";
}

int multiply(int a, int b, int c)
{
    return a * b * c;
}

int divide(int a, int b, int c)
{
    return a / b / c;
}
int mult2x(int a, int b)
{
   cout << "Mult2x gives " << 2 * (a * b) << endl;
}
int printTime(int a, int b)
{
   cout << "Time in minutes: " << a << endl;
   cout << "Time in seconds: " << b << endl;
}
*/


int main()
{
    /*
    void (*funcPointer)() = sayHello;
    (*funcPointer)();
    int (*ptr1)(int, int, int) = divide;
    cout << endl;
    cout << (*ptr1)(150,2,3);
    */

    /*
    int(*ptr)(int, int, int) = divide;
    cout << (*ptr1) (200, 30, 40) << endl;
    ptr1 = multiply;
    cout << (ptr1)(200, 30, 40);
    */

   /*
   void (*operations[4])(int, int) = {
        multiply,
        divide,
        mult2x,
        printTime,
   }
   int num1 = 2;
   int num2 = 5;

   for(int i = 0; i < 4; i++)
   {
    (*operations[i](num1, num2));
   };
*/



}